var typed = new Typed('#typed-content', {
    strings: ['The best preperation for tomorrow^700', '', 'Is doing your best today^700', '', 'Lets do our best work^1500 together^2000', ],
    smartBackspace: true,
    startDelay: 1000,
    backDelay: 1000,
    typeSpeed: 25,
    backSpeed: 20
    // onComplete: function(self) { prettyLog('onCmplete ' + self) }
  });
